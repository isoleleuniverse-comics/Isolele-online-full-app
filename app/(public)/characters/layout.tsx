export const metadata = {
  title: "Character | ISOLELE Universe",
  description: "Discover the heroes and legends of the ISOLELE universe",
}

export default function CharactersLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
